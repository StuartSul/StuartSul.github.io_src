# Custom collections
collections:
  blog:
    output: true
  tools:
    output: true

# HTML Compression settings
compress_html:
  clippings: all
  comments: all

# Build settings
sass:
  style: compressed

# Ignore files
exclude:
- Dockerfile
- README.md
- 'Gem*'
- '*.sh'
